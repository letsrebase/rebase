# The flake declares x86_64-linux only (its renderer binaries are the upstream Linux
# releases); on any other machine direnv would fail on every cd, so this is a no-op there.
if [ "$(uname -sm)" = "Linux x86_64" ]; then
  use flake
fi
